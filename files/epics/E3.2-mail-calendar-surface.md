# E3.2 — Mail/Calendar Surface

- **Phase:** 3 — Applications
- **Status:** TODO
- **Depends on:** E1.3, E1.9, E2.4 (apply the golden path)
- **Feeds:** E2.3 (seed data), E4.1 (recruiter flow)

## Goal
The inbox/calendar the recruiter actually opens.

## Scope
Stand up the mail + calendar UI the recruiter uses, wired to SSO and fed by the seed-data
generator (E2.3). Remember E1.9's insight: internal mail is pre-seeded; only the outbound invite
needs real deliverability.

## Deliverable
A working mail/calendar surface, SSO-wired, deployed via the golden path, ready to be seeded.

## Definition of Done
- [ ] Mail + calendar UI deployed via the golden path.
- [ ] SSO-wired.
- [ ] Accepts seed data from E2.3.
- [ ] Recruiter can open a populated inbox + calendar.
