apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# App HelmReleases are added here as epics land (E1.3 Keycloak, E3.1 Nextcloud, etc.)
# Each entry is a HelmRelease that uses ${company_id} and ${company_domain} from company-vars.
resources:
  - keycloak.yaml
  - nextcloud.yaml
